services:
  - type: web
    name: moviefinder
    runtime: node
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: TMDB_API_KEY
        sync: false
      - key: JWT_SECRET
        generateValue: true
